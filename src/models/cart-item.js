class CartItem {
  constructor(quantity, cardPrice, cardTitle, sum) {
    this.quantity = quantity;
    this.cardPrice = cardPrice;
    this.cardTitle = cardTitle;
    this.sum = sum;
  }
}

export default CartItem;
